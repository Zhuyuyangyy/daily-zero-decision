[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    data: tests for JSONL data integrity
    stats: tests for statistical computation
    provenance: tests for provenance logic
    paper: tests for paper modification scripts
    integration: integration tests
