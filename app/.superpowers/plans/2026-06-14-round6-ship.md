# Round 6 — 产品上线总收口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 daily-zero-decision 从 Round 5 推进到 v0.1.0 上线版本，锁死"每天只做这一小步"的产品主线，5 个并发分支（product-logic → sky-scene-infra → today-integration → docs-ci-deploy → qa-polish）合并后产物可直接发版。

**Architecture:** 自下而上三层。
1. **数据/逻辑层**（Issue 4 / product-logic）：parseTaskFromInput / handleEasier 守卫 / importState backfill / loadState 三态规则。
2. **视觉/组件层**（Issue 1 / sky-scene-infra + Issue 2 / today-integration）：抽出无业务 SkyScene + SkyHeaderContent + SkyProgressMini + useReducedMotion；CloudGarden 拆 mode='today'；toCloudGardenMood 工具。
3. **集成/工程层**（Issue 3 / today-integration + Issue 5 / docs-ci-deploy + Issue 6 / qa-polish）：TodayPage 接入 SkyScene / CI workflow / vercel.json / README + CHANGELOG / window.onerror / SettingsPage 浮层 / 一次性变更浮层 / QA 矩阵回归。

**Tech Stack:** React 18 + TypeScript + Vite 5 + Tailwind CSS 3 + Vitest 4 + GitHub Actions + Vercel。

**Spec 引用：** `app/docs/superpowers/specs/2026-06-14-round6-product-closure-design.md`（1250 行，下称 "spec"）。本 plan 是 spec 的执行清单，不是替代品——所有"为什么"在 spec，本 plan 只回答"做什么 / 怎么验 / 怎么 commit"。

**仓库布局（重要！仓库根 = `D:/ZYY Project`，app/ 是子目录）：**
- 所有"项目内"路径（README/CHANGELOG/CI workflow/package.json/src/*）字面前缀 `app/`
- `vercel.json` 在仓库根（不带 `app/` 前缀）
- CI runner 用 `working-directory: app` 切到子项目根

---

## File Structure（实现前最终确认）

### 新增文件（按归属分支）

**Issue 1（sky-scene-infra）独占：**
- `app/src/hooks/useReducedMotion.ts`（H1/M1：prefers-reduced-motion hook）
- `app/src/components/sky/SkyScene.tsx`（§3.2 SkyScene 主组件）
- `app/src/components/sky/SkyHeaderContent.tsx`（§3.4）
- `app/src/components/sky/SkyProgressMini.tsx`（§3.5）
- `app/src/index.css`（M1 强制位置：reduced-motion 兜底写入顶部，**不**放 *.module.css）
- `app/src/hooks/__tests__/useReducedMotion.test.ts`（§6.x 测试基线）
- `app/src/components/sky/__tests__/SkyScene.test.tsx`（§6.x 测试基线）

**Issue 2（today-integration）独占：**
- `app/src/utils/cloudGardenMood.ts`（M3：toCloudGardenMood 工具）

**Issue 3（today-integration）独占：**
- 修改 `app/src/pages/TodayPage.tsx`（H5 精确文案替换 + §4.1 SkyScene 接入）
- 修改 `app/src/components/today/CloudGarden.tsx`（§3.6 mode='today' 重构 + 内嵌背景迁出）
- 修改 `app/src/utils/skyMood.ts`（如需加新方法；保守不动）

**Issue 4（product-logic）独占：**
- 修改 `app/src/hooks/useTasks.ts`（H4 守卫四态决策表 + L5 显式 saveState）
- 修改 `app/src/utils/storage.ts`（H8 backfill + H9 onboarded 三态）
- 新增 `app/src/utils/__tests__/storage.test.ts`（H1 收口：qa-polish 不得新增）
- 新增 `app/src/hooks/__tests__/useTasks.test.ts`（§6.x 测试基线）

**Issue 5（docs-ci-deploy）独占：**
- 新增 `app/.github/workflows/ci.yml`（§6.2）
- 修改 `app/package.json`（M9 engines 字段 + M12 typecheck 脚本）
- 新增 `app/CHANGELOG.md`（L1 收口）
- 修改 `app/README.md`（§6.1 新定位）
- 修改 `app/src/pages/SettingsPage.tsx`（§14.6 第 1 条：v0.1.0 更新区块）
- 修改 `app/src/App.tsx`（§14.6 第 2 条：一次性浮层）
- 修改 `app/src/main.tsx`（M2 强制唯一 window.onerror 挂载点）
- 新增仓库根 `vercel.json`（§6.3）

**qa-polish 不新增任何文件，只跑测试 + 改 `test/` 配置。**

---

## Task 1：基础设施 — base 分支与并发分支

**Files:**
- 不修改任何应用代码

- [ ] **Step 1: 确认 base 分支是 `feature/scroll-optimization`**

```bash
cd /d/ZYY\ Project
git rev-parse --abbrev-ref HEAD
# 期望: feature/scroll-optimization
# 若不是: git checkout feature/scroll-optimization
```

- [ ] **Step 2: 拉最新**

```bash
cd /d/ZYY\ Project
git pull origin feature/scroll-optimization --ff-only
```

- [ ] **Step 3: 创建 5 条并发分支**

```bash
cd /d/ZYY\ Project
for branch in product-logic sky-scene-infra today-integration docs-ci-deploy qa-polish; do
  git checkout -b "round6/${branch}" feature/scroll-optimization
  git checkout feature/scroll-optimization
done
```

- [ ] **Step 4: 验证**

```bash
cd /d/ZYY\ Project
git branch | grep round6
# 期望 5 条: round6/product-logic / round6/sky-scene-infra / round6/today-integration / round6/docs-ci-deploy / round6/qa-polish
```

- [ ] **Step 5: 暂不 commit**（分支创建是本地操作，无需 commit）

---

## Task 2：product-logic — parseTaskFromInput time 推算（Round 6D 入口）

**Branch:** `round6/product-logic`
**Files:**
- Modify: `app/src/utils/storage.ts:155-204`
- Test: `app/src/utils/__tests__/storage.test.ts`（新增）

> Spec §5.1 / §7.3 期望 6 个 parseTaskFromInput 用例全部正确。`parseTaskFromInput` 返回类型保持 `Partial<Task>` 不变（spec line 550 警示：禁止改窄为 4 字段对象）。

- [ ] **Step 1: 写失败测试**

`app/src/utils/__tests__/storage.test.ts`：

```ts
import { describe, it, expect } from 'vitest';
import { parseTaskFromInput, importState } from '../storage';

describe('parseTaskFromInput', () => {
  it('读 2 页书 → reading, 5 分钟, 2 页', () => {
    const r = parseTaskFromInput('读 2 页书');
    expect(r.type).toBe('reading');
    expect(r.pagesPerSession).toBe(2);
    expect(r.time).toBe('5 分钟');
  });

  it('出门走走 5 分钟 → exercise, 5 分钟', () => {
    const r = parseTaskFromInput('出门走走 5 分钟');
    expect(r.type).toBe('exercise');
    expect(r.time).toBe('5 分钟');
  });

  it('看 5 分钟代码 → coding, 5 分钟', () => {
    const r = parseTaskFromInput('看 5 分钟代码');
    expect(r.type).toBe('coding');
    expect(r.time).toBe('5 分钟');
  });

  it('背 5 个新词 → reading, 单词本, 5 分钟 (背单词特例)', () => {
    const r = parseTaskFromInput('背 5 个新词');
    expect(r.type).toBe('reading');
    expect(r.bookName).toBe('单词本');
    expect(r.time).toBe('5 分钟'); // 背单词特例：绕过页数推算
  });

  it('写一行日记 → other, 3 分钟', () => {
    const r = parseTaskFromInput('写一行日记');
    expect(r.type).toBe('other');
    expect(r.time).toBe('3 分钟');
  });

  it('深呼吸三次 → other, 1 分钟 (深呼吸特例)', () => {
    const r = parseTaskFromInput('深呼吸三次');
    expect(r.type).toBe('other');
    expect(r.time).toBe('1 分钟');
  });
});

describe('importState', () => {
  it('坏 JSON 返回 null', () => {
    expect(importState('not json')).toBeNull();
  });

  it('空对象返回 null（缺 log 字段）', () => {
    expect(importState('{}')).toBeNull();
  });

  it('缺字段 fallback: 仅 log + streak', () => {
    const r = importState('{"log":["2026-06-13"],"streak":{"current":1,"best":1,"lastCompletedDate":"2026-06-13"}}');
    expect(r).not.toBeNull();
    expect(r!.log).toEqual(['2026-06-13']);
    expect(r!.streak.current).toBe(1);
  });

  it('onboarded 缺失 → 走 defaultState.onboarded = false（不被强制 true）', () => {
    const r = importState('{"log":["2026-06-13"],"streak":{"current":1,"best":1,"lastCompletedDate":"2026-06-13"}}');
    expect(r!.onboarded).toBe(false);
  });

  it('H8 backfill: 历史 type=other 但 title 含"读" → 升级为 reading', () => {
    const json = JSON.stringify({
      log: ['2026-06-10'],
      streak: { current: 1, best: 1, lastCompletedDate: '2026-06-10' },
      history: {
        '2026-06-10': [{ id: '1', title: '读 2 页书', type: 'other', createdAt: '2026-06-10', completedAt: '2026-06-10' }]
      }
    });
    const r = importState(json);
    expect(r!.history['2026-06-10'][0].type).toBe('reading');
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /d/ZYY\ Project/app
npm test -- src/utils/__tests__/storage.test.ts
# 期望: 大量 FAIL（因为 parseTaskFromInput 还没补 time 推算，importState 还没 backfill）
```

- [ ] **Step 3: 修改 parseTaskFromInput 末尾补 time 推算**

`app/src/utils/storage.ts:155-204` 现有函数体末尾（return 之前）追加：

```ts
  // === Round 6 H2/H8: time 推算（不修改 Partial<Task> 返回类型）===
  // 优先级: minuteMatch > 背单词特例 > pagesPerSession 推算 > 深呼吸特例 > type 默认
  if (minuteMatch) {
    time = `${Number(minuteMatch[1])} 分钟`;
  } else if (lower.includes('深呼吸')) {
    time = '1 分钟';
  } else if (lower.includes('新词') || lower.includes('背词') || lower.includes('单词')) {
    // 背单词特例：绕过 pagesPerSession 推算
    time = '5 分钟';
  } else if (type === 'reading' || type === 'exercise' || type === 'coding') {
    time = pagesPerSession && pagesPerSession <= 2 ? '5 分钟'
         : pagesPerSession && pagesPerSession <= 10 ? '15 分钟'
         : '30 分钟';
  } else {
    // type === 'other' 且无 minute / 关键词特例
    time = '3 分钟';
  }

  return {
    type,
    bookName: bookName || undefined,
    pagesPerSession,
    startPage,
    time,
  };
```

- [ ] **Step 4: 修改 importState 加 H8 backfill + 确认 H9 三态**

`app/src/utils/storage.ts:67-90` 替换为：

```ts
export function importState(json: string): AppState | null {
  try {
    const parsed = JSON.parse(json);

    if (!Array.isArray(parsed.log)) return null;
    if (!parsed.streak || typeof parsed.streak.current !== 'number') return null;

    // H9: onboarded 三态规则（true→true, false→false, undefined→走 defaultState.onboarded 即 false）
    let onboarded: boolean;
    if (parsed.onboarded === true) onboarded = true;
    else if (parsed.onboarded === false) onboarded = false;
    else onboarded = defaultState.onboarded; // 缺字段走 false，不强制 true

    // H8: 老用户 backfill - 对 history[*] 与 parsed.tasks 中 type='other' 但 title 含
    // 阅读/散步/编码 关键词的，升级 type
    const backfillType = (task: any) => {
      if (task.type !== 'other') return task;
      const lower = (task.title || '').toLowerCase();
      if (lower.includes('书') || lower.includes('读') || lower.includes('页') || lower.includes('新词') || lower.includes('单词')) {
        return { ...task, type: 'reading' as const };
      }
      if (lower.includes('走') || lower.includes('跑') || lower.includes('运动') || lower.includes('健身') || lower.includes('瑜伽')) {
        return { ...task, type: 'exercise' as const };
      }
      if (lower.includes('代码') || lower.includes('编程')) {
        return { ...task, type: 'coding' as const };
      }
      return task;
    };

    const history: Record<string, Task[]> = {};
    if (parsed.history && typeof parsed.history === 'object') {
      for (const [date, tasks] of Object.entries(parsed.history)) {
        history[date] = Array.isArray(tasks) ? (tasks as any[]).map(backfillType) : [];
      }
    }
    const tasks = Array.isArray(parsed.tasks) ? (parsed.tasks as any[]).map(backfillType) : [];

    return {
      ...defaultState,
      ...parsed,
      tasks,
      settings: {
        ...defaultState.settings,
        ...(parsed.settings || {}),
      },
      achievements: parsed.achievements || [],
      history,
      moods: parsed.moods || {},
      pomodoroSessions: parsed.pomodoroSessions ?? 0,
      onboarded,
    };
  } catch {
    return null;
  }
}
```

- [ ] **Step 5: 跑测试确认通过**

```bash
cd /d/ZYY\ Project/app
npm test -- src/utils/__tests__/storage.test.ts
# 期望: 全部 PASS
```

- [ ] **Step 6: 跑全量测试，确认无回归**

```bash
cd /d/ZYY\ Project/app
npm test
# 期望: 全部 PASS（含原 skyMood.test.ts）
```

- [ ] **Step 7: Commit**

```bash
cd /d/ZYY\ Project
git add app/src/utils/storage.ts app/src/utils/__tests__/storage.test.ts
git commit -m "fix(tasks): parseTaskFromInput time 推断 + importState backfill & 三态 onboarded"
```

---

## Task 3：product-logic — useTasks handleEasier MAX 守卫 + 显式 saveState

**Branch:** `round6/product-logic`
**Files:**
- Modify: `app/src/hooks/useTasks.ts:191-212`（H4 守卫）
- Modify: `app/src/hooks/useTasks.ts:240`（L5 显式 saveState）
- Test: `app/src/hooks/__tests__/useTasks.test.ts`（新增）

> Spec §5.3 H4 四态决策表：0 张 → seed；1 张 incomplete → return prev；1 张 completed → return prev；2+ 张 → return prev + warn。原代码 `!t.completedAt` 过滤会漏掉 completed 任务——必须修复。

- [ ] **Step 1: 写失败测试**

`app/src/hooks/__tests__/useTasks.test.ts`：

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import { renderHook, act } from '@testing-library/react';
import { useTasks } from '../useTasks';
import type { AppState } from '../../types';

const makeState = (overrides: Partial<AppState> = {}): AppState => ({
  tasks: [],
  log: [],
  streak: { current: 0, best: 0, lastCompletedDate: null },
  settings: { defaultPagesPerSession: 10, lastPageRead: 0, lastBookName: '', customPresets: [] },
  achievements: [],
  history: {},
  moods: {},
  pomodoroSessions: 0,
  onboarded: true,
  ...overrides,
});

describe('useTasks.handleEasier MAX 守卫', () => {
  it('0 张任务 → seed 1 张', () => {
    const state = makeState();
    const setState = (updater: any) => Object.assign(state, typeof updater === 'function' ? updater(state) : updater);
    const { result } = renderHook(() => useTasks(state, setState, () => {}));
    act(() => result.current.handleEasier());
    expect(state.tasks.length).toBe(1);
  });

  it('1 张 incomplete → return prev（不允许再养）', () => {
    const today = new Date().toISOString().slice(0, 10);
    const incomplete = { id: '1', title: '读 2 页书', type: 'reading' as const, createdAt: today };
    const state = makeState({ tasks: [incomplete] });
    const setState = (updater: any) => Object.assign(state, typeof updater === 'function' ? updater(state) : updater);
    const { result } = renderHook(() => useTasks(state, setState, () => {}));
    act(() => result.current.handleEasier());
    // 标题应该被改写（map 分支），但任务数仍为 1
    expect(state.tasks.length).toBe(1);
    expect(state.tasks[0].title).toContain('读'); // 标题被 makeEasierTitle 重写
  });

  it('1 张 completed → return prev（**不**生成第 2 张）', () => {
    const today = new Date().toISOString().slice(0, 10);
    const completed = { id: '1', title: '读 2 页书', type: 'reading' as const, createdAt: today, completedAt: today };
    const state = makeState({ tasks: [completed] });
    const setState = (updater: any) => Object.assign(state, typeof updater === 'function' ? updater(state) : updater);
    const { result } = renderHook(() => useTasks(state, setState, () => {}));
    act(() => result.current.handleEasier());
    expect(state.tasks.length).toBe(1); // 关键：仍是 1 张，不生成第 2 张
  });

  it('addWithValue 第二张卡被拒绝', () => {
    const today = new Date().toISOString().slice(0, 10);
    const existing = { id: '1', title: '读 2 页书', type: 'reading' as const, createdAt: today };
    const state = makeState({ tasks: [existing] });
    const setState = (updater: any) => Object.assign(state, typeof updater === 'function' ? updater(state) : updater);
    const { result } = renderHook(() => useTasks(state, setState, () => {}));
    act(() => result.current.addWithValue('深呼吸三次'));
    expect(state.tasks.length).toBe(1); // 关键：第二张被拒绝
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /d/ZYY\ Project/app
npm test -- src/hooks/__tests__/useTasks.test.ts
# 期望: "1 张 completed" 和 "addWithValue 第二张" 用例 FAIL（原代码用 !t.completedAt 过滤会漏掉 completed 任务）
```

- [ ] **Step 3: 替换 handleEasier 守卫代码（§5.3 H4 修复后版本）**

`app/src/hooks/useTasks.ts:171-231` 替换 `handleEasier` 函数体（替换原 191-212 setState updater 部分）：

```ts
  const handleEasier = useCallback(() => {
    const LIGHT_TEMPLATES: Array<{ title: string; type: Task['type']; minutes: number; pages?: number; place: string }> = [
      { title: '读 2 页书', type: 'reading', minutes: 5, pages: 2, place: '沙发' },
      { title: '出门走走', type: 'exercise', minutes: 10, place: '楼下' },
      { title: '看 5 分钟代码', type: 'coding', minutes: 5, place: '电脑前' },
      { title: '深呼吸三次', type: 'other', minutes: 1, place: '原地' },
      { title: '写一行日记', type: 'other', minutes: 3, place: '桌前' },
    ];

    const halved = (n: number) => Math.max(1, Math.floor(n / 2));

    const makeEasierTitle = (task: Task, newMinutes: number, newPages?: number): string => {
      if (task.type === 'reading' && newPages) return `读 ${newPages} 页书`;
      if (task.type === 'coding') return `看 ${newMinutes} 分钟代码`;
      if (task.type === 'exercise') return `出门走走 ${newMinutes} 分钟`;
      return task.title;
    };

    setState((prev) => {
      // H4 修复后：所有今日任务（incomplete + completed）都计入
      const allTodays = prev.tasks.filter((t) => t.createdAt === today);
      const todaysCount = allTodays.length;
      const hasIncomplete = allTodays.some((t) => !t.completedAt);

      // 2+ 张：违反 MAX，return prev + 警告
      if (todaysCount > MAX_TASKS_PER_DAY) {
        console.warn(`[handleEasier] tasks 数组今日项数 (${todaysCount}) 超过 MAX (${MAX_TASKS_PER_DAY})`);
        return prev;
      }

      // 1 张 completed：今天已完成，不要再添加
      if (todaysCount === 1 && !hasIncomplete) {
        return prev;
      }

      // 0 张任务：seed 一次
      if (todaysCount === 0) {
        const seed = prev.tasks.length;
        const tpl = LIGHT_TEMPLATES[seed % LIGHT_TEMPLATES.length];
        const newTask: Task = {
          id: generateId(),
          title: tpl.title,
          type: tpl.type,
          bookName: tpl.type === 'reading' ? prev.settings.lastBookName : undefined,
          startPage: tpl.pages ? prev.settings.lastPageRead + 1 : undefined,
          endPage: tpl.pages ? prev.settings.lastPageRead + tpl.pages : undefined,
          currentPage: tpl.pages ? prev.settings.lastPageRead + tpl.pages : undefined,
          pagesPerSession: tpl.pages,
          place: tpl.place,
          time: `${tpl.minutes} 分钟`,
          createdAt: today,
        };
        return { ...prev, tasks: [...prev.tasks, newTask] };
      }

      // 1 张 incomplete：按 makeEasierTitle 重写主标题
      return {
        ...prev,
        tasks: prev.tasks.map((t) => {
          if (t.completedAt) return t; // 安全兜底
          const minMatch = t.time?.match(/(\d+)/);
          const newMinutes = minMatch ? halved(parseInt(minMatch[1], 10)) : 5;
          const newPages = t.pagesPerSession ? halved(t.pagesPerSession) : undefined;
          return {
            ...t,
            title: makeEasierTitle(t, newMinutes, newPages),
            time: `${newMinutes} 分钟`,
            pagesPerSession: newPages,
            endPage: newPages && t.startPage ? t.startPage + newPages - 1 : t.endPage,
            currentPage: newPages && t.startPage ? t.startPage + newPages - 1 : t.currentPage,
          };
        }),
      };
    });
  }, [today, setState]);
```

- [ ] **Step 4: 显式 saveState 在 handleOnboardingFinish（L5）**

`app/src/hooks/useTasks.ts:240` 替换：

```ts
  const handleOnboardingFinish = useCallback(() => {
    setState((prev) => ({ ...prev, onboarded: true }));
    // L5: 显式 saveState 避免 useEffect 异步竞态（用户在 useEffect 触发前刷新会重弹 Onboarding）
    setTimeout(() => {
      try {
        const stored = localStorage.getItem('daily-zero-decision');
        if (stored) {
          const parsed = JSON.parse(stored);
          parsed.onboarded = true;
          localStorage.setItem('daily-zero-decision', JSON.stringify(parsed));
        }
      } catch (e) {
        console.warn('[handleOnboardingFinish] failed to save', e);
      }
    }, 0);
  }, [setState]);
```

- [ ] **Step 5: 跑测试确认通过**

```bash
cd /d/ZYY\ Project/app
npm test -- src/hooks/__tests__/useTasks.test.ts
# 期望: 全部 PASS
```

- [ ] **Step 6: 跑全量测试**

```bash
cd /d/ZYY\ Project/app
npm test
# 期望: 全部 PASS
```

- [ ] **Step 7: Commit**

```bash
cd /d/ZYY\ Project
git add app/src/hooks/useTasks.ts app/src/hooks/__tests__/useTasks.test.ts
git commit -m "fix(tasks): handleEasier 四态守卫 (H4) + handleOnboardingFinish 显式 saveState (L5)"
```

---

## Task 4：product-logic — loadState 三态注释 + backfill（接 §6.x 现状澄清）

**Branch:** `round6/product-logic`
**Files:**
- Modify: `app/src/utils/storage.ts:26-47`（H8 loadState 同步 backfill + H9 三态注释）

> loadState 与 importState 走同一条 backfill 逻辑，避免老用户 reload 后被 handleEasier 误判（spec L1 风险）。

- [ ] **Step 1: 修改 loadState**

`app/src/utils/storage.ts:26-47` 替换：

```ts
export function loadState(): AppState {
  try {
    const stored = localStorage.getItem(STORAGE_KEY);
    if (!stored) return defaultState;
    const parsed = JSON.parse(stored);
    // Migrate old single-task format
    if (parsed.task && !parsed.tasks) {
      parsed.tasks = parsed.task ? [parsed.task] : [];
      delete parsed.task;
    }
    if (!parsed.achievements) parsed.achievements = [];
    if (!parsed.history) parsed.history = {};
    if (!parsed.settings) parsed.settings = defaultState.settings;
    if (!parsed.settings.customPresets) parsed.settings.customPresets = [];
    if (!parsed.moods) parsed.moods = {};
    if (parsed.pomodoroSessions == null) parsed.pomodoroSessions = 0;
    // H9 三态规则: parsed.onboarded == null 走 defaultState.onboarded = false
    // 不要改成 ?? true 强制标记为已 onboarded（会跳过 Onboarding 引导）
    if (parsed.onboarded == null) parsed.onboarded = false;

    // H8: 老用户 backfill - 对历史 tasks / history 同样升级 type
    const backfillType = (task: any) => {
      if (!task || task.type !== 'other') return task;
      const lower = (task.title || '').toLowerCase();
      if (lower.includes('书') || lower.includes('读') || lower.includes('页') || lower.includes('新词') || lower.includes('单词')) {
        return { ...task, type: 'reading' };
      }
      if (lower.includes('走') || lower.includes('跑') || lower.includes('运动') || lower.includes('健身') || lower.includes('瑜伽')) {
        return { ...task, type: 'exercise' };
      }
      if (lower.includes('代码') || lower.includes('编程')) {
        return { ...task, type: 'coding' };
      }
      return task;
    };
    if (Array.isArray(parsed.tasks)) {
      parsed.tasks = parsed.tasks.map(backfillType);
    }
    if (parsed.history && typeof parsed.history === 'object') {
      for (const [date, tasks] of Object.entries(parsed.history)) {
        if (Array.isArray(tasks)) {
          (parsed.history as any)[date] = (tasks as any[]).map(backfillType);
        }
      }
    }

    return { ...defaultState, ...parsed };
  } catch {
    return defaultState;
  }
}
```

- [ ] **Step 2: 跑全量测试确认无回归**

```bash
cd /d/ZYY\ Project/app
npm test
# 期望: 全部 PASS
```

- [ ] **Step 3: Commit**

```bash
cd /d/ZYY\ Project
git add app/src/utils/storage.ts
git commit -m "fix(storage): loadState 同步 H8 backfill + H9 三态 onboarded 注释"
```

---

## Task 5：product-logic — 合并准备

**Branch:** `round6/product-logic`

- [ ] **Step 1: 推送到 origin（如果远程存在）**

```bash
cd /d/ZYY\ Project
git push origin round6/product-logic 2>/dev/null || echo "remote not set, skip push (merge will be local)"
```

- [ ] **Step 2: 切回 base 合并**

```bash
cd /d/ZYY\ Project
git checkout feature/scroll-optimization
git merge --no-ff round6/product-logic -m "Merge round6/product-logic: 解析守卫 + 数据 backfill"
# 期望: 无冲突（独占 storage.ts / useTasks.ts / 测试文件）
```

- [ ] **Step 3: 跑全量测试**

```bash
cd /d/ZYY\ Project/app
npm test
npm run typecheck
npm run build
# 期望: 全部通过
```

- [ ] **Step 4: 暂不推送到 main**（等其他分支完成 §9.1 合并顺序）

---

## Task 6：sky-scene-infra — useReducedMotion hook

**Branch:** `round6/sky-scene-infra`
**Files:**
- Create: `app/src/hooks/useReducedMotion.ts`
- Test: `app/src/hooks/__tests__/useReducedMotion.test.ts`

- [ ] **Step 1: 写失败测试**

`app/src/hooks/__tests__/useReducedMotion.test.ts`：

```ts
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { renderHook } from '@testing-library/react';
import { useReducedMotion } from '../useReducedMotion';

describe('useReducedMotion', () => {
  let matchMediaMock: any;

  beforeEach(() => {
    matchMediaMock = vi.fn().mockImplementation((query: string) => ({
      matches: query.includes('reduce'),
      media: query,
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
      addListener: vi.fn(),
      removeListener: vi.fn(),
    }));
    window.matchMedia = matchMediaMock;
  });

  afterEach(() => {
    vi.restoreAllMocks();
  });

  it('prefers-reduced-motion: reduce → 返回 true', () => {
    const { result } = renderHook(() => useReducedMotion());
    expect(result.current).toBe(true);
  });

  it('prefers-reduced-motion: no-preference → 返回 false', () => {
    matchMediaMock.mockImplementation((query: string) => ({
      matches: false,
      media: query,
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
      addListener: vi.fn(),
      removeListener: vi.fn(),
    }));
    const { result } = renderHook(() => useReducedMotion());
    expect(result.current).toBe(false);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /d/ZYY\ Project/app
npm test -- src/hooks/__tests__/useReducedMotion.test.ts
# 期望: FAIL (useReducedMotion not found)
```

- [ ] **Step 3: 实现 hook**

`app/src/hooks/useReducedMotion.ts`：

```ts
import { useEffect, useState } from 'react';

export function useReducedMotion(): boolean {
  const [reduced, setReduced] = useState(() => {
    if (typeof window === 'undefined') return false;
    return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  });

  useEffect(() => {
    if (typeof window === 'undefined') return;

    const mq = window.matchMedia('(prefers-reduced-motion: reduce)');
    const handler = (e: MediaQueryListEvent) => setReduced(e.matches);

    if (mq.addEventListener) {
      mq.addEventListener('change', handler);
      return () => mq.removeEventListener('change', handler);
    }

    mq.addListener(handler);
    return () => mq.removeListener(handler);
  }, []);

  return reduced;
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd /d/ZYY\ Project/app
npm test -- src/hooks/__tests__/useReducedMotion.test.ts
# 期望: PASS
```

- [ ] **Step 5: Commit**

```bash
cd /d/ZYY\ Project
git add app/src/hooks/useReducedMotion.ts app/src/hooks/__tests__/useReducedMotion.test.ts
git commit -m "feat(sky): add useReducedMotion hook (M1 强制路径)"
```

---

## Task 7：sky-scene-infra — reduced-motion CSS 兜底（M1 强制位置）

**Branch:** `round6/sky-scene-infra`
**Files:**
- Modify: `app/src/index.css`（在所有其他规则之前插入 @media 块）

> **M1 强制**：CSS 兜底必须写入 `src/index.css` 顶部，**禁止**放 `*.module.css`（否则非 sky-scene 组件的类名不会命中）。

- [ ] **Step 1: 读现有 index.css 头部**

```bash
cd /d/ZYY\ Project
head -20 app/src/index.css
```

- [ ] **Step 2: 在文件最顶部插入 @media 块（保留所有现有内容）**

`app/src/index.css` 文件最顶部插入：

```css
/* === Round 6 M1 强制：reduced-motion 兜底（必须全局加载，详见 spec §3.3）=== */
@media (prefers-reduced-motion: reduce) {
  .sky-bird,
  .sky-balloon,
  .sky-cloud-drift,
  .sky-sparkle,
  .sky-atmosphere,
  .clay-cloud-drift,
  .dawn-aura-breathe,
  .cloud-drift,
  .float,
  .sky-breathe {
    animation: none !important;
    transform: none !important;
  }
}

/* === 以下为原 index.css 内容 === */

```

（在 `@media` 块后追加原内容，保持所有现有 CSS 不变）

- [ ] **Step 3: 验证**

```bash
cd /d/ZYY\ Project
head -30 app/src/index.css
# 期望第一行是 @media 块，最后是 "=== 以下为原 index.css 内容 ===" 注释
```

- [ ] **Step 4: Commit**

```bash
cd /d/ZYY\ Project
git add app/src/index.css
git commit -m "feat(sky): add reduced-motion CSS fallback to global index.css (M1 强制位置)"
```

---

## Task 8：sky-scene-infra — SkyScene 组件（核心无业务天空）

**Branch:** `round6/sky-scene-infra`
**Files:**
- Create: `app/src/components/sky/SkyScene.tsx`
- Test: `app/src/components/sky/__tests__/SkyScene.test.tsx`

- [ ] **Step 1: 写失败测试**

`app/src/components/sky/__tests__/SkyScene.test.tsx`：

```tsx
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import { SkyScene } from '../SkyScene';

describe('SkyScene', () => {
  it('不读取 AppState / Task / useTasks / useStreak', () => {
    // 编译期检查：SkyScene.tsx 不 import 上述 hooks/types
    // （本测试仅做冒烟，确保 SkyScene 接受 mood/density/variant 渲染而不抛错）
    const { container } = render(
      <SkyScene mood="dawn" density="comfortable" variant="today">
        <div data-testid="child">child</div>
      </SkyScene>
    );
    expect(container.firstChild).not.toBeNull();
  });

  it('density=minimal 时不渲染 Birds / Balloon', () => {
    const { container } = render(
      <SkyScene mood="dawn" density="minimal" variant="today">
        <div>child</div>
      </SkyScene>
    );
    // Birds / Balloon 用 .sky-bird / .sky-balloon 类名
    expect(container.querySelector('.sky-bird')).toBeNull();
    expect(container.querySelector('.sky-balloon')).toBeNull();
  });

  it('variant=garden 时不渲染 ForegroundFade', () => {
    const { container } = render(
      <SkyScene mood="dawn" density="comfortable" variant="garden">
        <div>child</div>
      </SkyScene>
    );
    expect(container.querySelector('[data-sky-layer="foreground-fade"]')).toBeNull();
  });

  it('variant=today 时渲染 ForegroundFade', () => {
    const { container } = render(
      <SkyScene mood="dawn" density="comfortable" variant="today">
        <div>child</div>
      </SkyScene>
    );
    expect(container.querySelector('[data-sky-layer="foreground-fade"]')).not.toBeNull();
  });

  it('装饰层都有 aria-hidden=true', () => {
    const { container } = render(
      <SkyScene mood="dawn" density="comfortable" variant="today">
        <div>child</div>
      </SkyScene>
    );
    const decorativeLayers = container.querySelectorAll('[data-sky-layer]');
    decorativeLayers.forEach((el) => {
      expect(el.getAttribute('aria-hidden')).toBe('true');
    });
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /d/ZYY\ Project/app
npm test -- src/components/sky/__tests__/SkyScene.test.tsx
# 期望: FAIL (SkyScene not found)
```

- [ ] **Step 3: 实现 SkyScene**

`app/src/components/sky/SkyScene.tsx`：

```tsx
import { useEffect, useState, type ReactNode } from 'react';
import { useReducedMotion } from '../../hooks/useReducedMotion';
import { SkyHeaderContent } from './SkyHeaderContent';

export type SkyDensity = 'minimal' | 'comfortable' | 'rich';
export type SkyVariant = 'today' | 'garden';
export type SkyMood = 'dawn' | 'morning' | 'clear' | 'sunny' | 'golden';

// Sun 视觉参数表（spec §3.2 H3 占位值，Issue 1 落地时调优）
const SUN_PARAMS: Record<SkyMood, { top: string; color: string; glow: number }> = {
  dawn:    { top: '18%', color: '#FFB37A', glow: 0.5 },
  morning: { top: '22%', color: '#FFD89B', glow: 0.6 },
  clear:   { top: '28%', color: '#FFE4A8', glow: 0.55 },
  sunny:   { top: '32%', color: '#FFCB6B', glow: 0.7 },
  golden:  { top: '25%', color: '#FF9D5C', glow: 0.75 },
};

export interface SkySceneProps {
  mood: SkyMood;
  density: SkyDensity;
  variant: SkyVariant;
  reducedMotion?: boolean;
  className?: string;
  children?: ReactNode;
}

export function SkyScene({ mood, density, variant, reducedMotion: propReducedMotion, className, children }: SkySceneProps) {
  const hookReducedMotion = useReducedMotion();
  const reducedMotion = propReducedMotion ?? hookReducedMotion;

  // H12: viewport <= 375px 强制 density='minimal'
  const [isNarrow, setIsNarrow] = useState(() => {
    if (typeof window === 'undefined') return false;
    return window.innerWidth <= 375;
  });
  useEffect(() => {
    const onResize = () => setIsNarrow(window.innerWidth <= 375);
    window.addEventListener('resize', onResize);
    return () => window.removeEventListener('resize', onResize);
  }, []);

  const effectiveDensity: SkyDensity = isNarrow ? 'minimal' : density;
  const sun = SUN_PARAMS[mood];

  return (
    <div
      className={`clay-sky-scene ${className ?? ''}`}
      style={{
        position: 'relative',
        width: '100%',
        display: 'flex',
        flexDirection: 'column',
      }}
    >
      {/* SkyBackground — 5 层 radial + linear gradient */}
      <div
        data-sky-layer="background"
        aria-hidden="true"
        style={{
          position: 'absolute',
          inset: 0,
          pointerEvents: 'none',
          background: `
            radial-gradient(ellipse 40% 30% at 50% 10%, rgba(255, 245, 220, 0.6) 0%, transparent 70%),
            radial-gradient(ellipse 70% 50% at 50% 85%, rgba(255, 190, 110, 0.45) 0%, transparent 70%),
            radial-gradient(ellipse 60% 30% at 30% 70%, rgba(255, 215, 175, 0.40) 0%, transparent 60%),
            radial-gradient(ellipse 50% 25% at 70% 65%, rgba(255, 200, 165, 0.35) 0%, transparent 60%),
            linear-gradient(180deg, var(--sky-dawn-1, #FFE9DF) 0%, var(--sky-dawn-2, #FDF4F0) 45%, var(--sky-dawn-3, #FAE6DC) 100%)
          `,
        }}
      />

      {/* Sun — 山后 */}
      <div
        data-sky-layer="sun"
        aria-hidden="true"
        style={{
          position: 'absolute',
          top: sun.top,
          left: '50%',
          transform: 'translateX(-50%)',
          width: 80,
          height: 80,
          borderRadius: '50%',
          background: `radial-gradient(circle, ${sun.color} 0%, ${sun.color}88 40%, transparent 70%)`,
          opacity: sun.glow,
          pointerEvents: 'none',
        }}
      />

      {/* FarMountains */}
      {effectiveDensity !== 'minimal' && (
        <svg
          data-sky-layer="far-mountains"
          aria-hidden="true"
          viewBox="0 0 100 30"
          preserveAspectRatio="none"
          style={{ position: 'absolute', left: 0, right: 0, bottom: 0, width: '100%', height: '20%', pointerEvents: 'none' }}
        >
          <path d="M0 25 Q 15 15, 30 20 T 55 18 T 80 22 T 100 20 L 100 30 L 0 30 Z" fill="rgba(180, 130, 110, 0.25)" />
        </svg>
      )}

      {/* MidMountains — 仅 comfortable / rich */}
      {effectiveDensity !== 'minimal' && (
        <svg
          data-sky-layer="mid-mountains"
          aria-hidden="true"
          viewBox="0 0 100 30"
          preserveAspectRatio="none"
          style={{ position: 'absolute', left: 0, right: 0, bottom: 0, width: '100%', height: '15%', pointerEvents: 'none' }}
        >
          <path d="M0 22 Q 20 18, 40 20 T 70 19 T 100 21 L 100 30 L 0 30 Z" fill="rgba(150, 110, 90, 0.35)" />
        </svg>
      )}

      {/* CloudAtmosphere — 仅 comfortable / rich */}
      {effectiveDensity !== 'minimal' && (
        <div
          data-sky-layer="atmosphere"
          aria-hidden="true"
          className="sky-atmosphere"
          style={{
            position: 'absolute',
            inset: 0,
            pointerEvents: 'none',
            background: 'radial-gradient(ellipse 80% 30% at 50% 60%, rgba(255, 255, 255, 0.15) 0%, transparent 70%)',
          }}
        />
      )}

      {/* Birds — comfortable: 2-3 只; rich: 5-7 只 */}
      {(effectiveDensity === 'comfortable' || effectiveDensity === 'rich') && !reducedMotion && (
        <div data-sky-layer="birds" aria-hidden="true" style={{ position: 'absolute', inset: 0, pointerEvents: 'none' }}>
          {Array.from({ length: effectiveDensity === 'rich' ? 6 : 3 }).map((_, i) => (
            <span
              key={i}
              className="sky-bird"
              style={{
                position: 'absolute',
                top: `${20 + i * 8}%`,
                left: `${10 + i * 15}%`,
                fontSize: 14,
                opacity: 0.6,
              }}
            >
              🕊
            </span>
          ))}
        </div>
      )}

      {/* Balloon — 仅 rich */}
      {effectiveDensity === 'rich' && !reducedMotion && (
        <div
          data-sky-layer="balloon"
          aria-hidden="true"
          className="sky-balloon"
          style={{
            position: 'absolute',
            top: '15%',
            right: '20%',
            fontSize: 24,
            pointerEvents: 'none',
          }}
        >
          🎈
        </div>
      )}

      {/* children — SkyHeaderContent / CloudGarden 等 */}
      <div style={{ position: 'relative', zIndex: 10 }}>{children}</div>

      {/* GrassLine — 永远渲染 */}
      <div
        data-sky-layer="grass"
        aria-hidden="true"
        style={{
          position: 'absolute',
          left: 0,
          right: 0,
          bottom: 0,
          height: variant === 'today' ? 12 : 6,
          background: variant === 'today'
            ? 'linear-gradient(180deg, transparent 0%, rgba(180, 200, 150, 0.5) 100%)'
            : 'rgba(180, 200, 150, 0.3)',
          pointerEvents: 'none',
        }}
      />

      {/* ForegroundFade — 仅 variant='today' */}
      {variant === 'today' && (
        <div
          data-sky-layer="foreground-fade"
          aria-hidden="true"
          style={{
            position: 'absolute',
            left: 0,
            right: 0,
            bottom: 0,
            height: 80,
            background: 'linear-gradient(180deg, transparent 0%, var(--bg-page, #FDF4F0) 100%)',
            pointerEvents: 'none',
            zIndex: 5,
          }}
        />
      )}
    </div>
  );
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd /d/ZYY\ Project/app
npm test -- src/components/sky/__tests__/SkyScene.test.tsx
# 期望: PASS
```

- [ ] **Step 5: Commit**

```bash
cd /d/ZYY\ Project
git add app/src/components/sky/SkyScene.tsx app/src/components/sky/__tests__/SkyScene.test.tsx
git commit -m "feat(sky): add SkyScene component (density/variant/H12/reduced-motion)"
```

---

## Task 9：sky-scene-infra — SkyHeaderContent + SkyProgressMini

**Branch:** `round6/sky-scene-infra`
**Files:**
- Create: `app/src/components/sky/SkyHeaderContent.tsx`
- Create: `app/src/components/sky/SkyProgressMini.tsx`

- [ ] **Step 1: 实现 SkyHeaderContent**

`app/src/components/sky/SkyHeaderContent.tsx`：

```tsx
import type { ReactNode } from 'react';

export interface SkyHeaderContentProps {
  title?: string;
  subtitle?: string;
  children?: ReactNode;
}

export function SkyHeaderContent({ title, subtitle, children }: SkyHeaderContentProps) {
  return (
    <div style={{ textAlign: 'center', padding: '8px 16px' }}>
      <h1
        style={{
          fontFamily: 'var(--font-display)',
          fontSize: 20,
          fontWeight: 700,
          color: 'var(--ink)',
          margin: 0,
        }}
      >
        {title ?? '今天只做这一小步'}
      </h1>
      {subtitle !== undefined && (
        <p style={{ color: 'var(--ink-light)', fontSize: 12, margin: '4px 0 0' }}>{subtitle}</p>
      )}
      {children && <div style={{ marginTop: 8 }}>{children}</div>}
    </div>
  );
}
```

- [ ] **Step 2: 实现 SkyProgressMini**

`app/src/components/sky/SkyProgressMini.tsx`：

```tsx
import type { SkyMood } from './SkyScene';

export interface SkyProgressMiniProps {
  streak: number;
  totalClouds: number;
  mood: SkyMood;
  hasTodayCloud: boolean;
}

const MOOD_ICON: Record<SkyMood, string> = {
  dawn: '🌅',
  morning: '🌄',
  clear: '🌤️',
  sunny: '☀️',
  golden: '☀️',
};

function pickText(streak: number, totalClouds: number, hasTodayCloud: boolean): string {
  if (hasTodayCloud) {
    return `☁️ 今天已长出 · 连续 ${streak} 天`;
  }
  if (totalClouds > 0) {
    return `☁️ 今天待长出 · 累计 ${totalClouds} 朵`;
  }
  return '☁️ 第一朵云，今天开始';
}

export function SkyProgressMini({ streak, totalClouds, mood, hasTodayCloud }: SkyProgressMiniProps) {
  const icon = MOOD_ICON[mood];
  const text = pickText(streak, totalClouds, hasTodayCloud);
  return (
    <div
      style={{
        display: 'inline-flex',
        alignItems: 'center',
        gap: 6,
        padding: '4px 12px',
        borderRadius: 999,
        background: 'rgba(255, 255, 255, 0.6)',
        backdropFilter: 'blur(8px)',
        fontSize: 12,
        color: 'var(--ink)',
        fontWeight: 600,
      }}
    >
      <span>{icon}</span>
      <span>{text}</span>
    </div>
  );
}
```

- [ ] **Step 3: 跑全量测试，确认无回归**

```bash
cd /d/ZYY\ Project/app
npm test
# 期望: PASS
```

- [ ] **Step 4: Commit**

```bash
cd /d/ZYY\ Project
git add app/src/components/sky/SkyHeaderContent.tsx app/src/components/sky/SkyProgressMini.tsx
git commit -m "feat(sky): add SkyHeaderContent and SkyProgressMini components"
```

---

## Task 10：sky-scene-infra — 合并准备

**Branch:** `round6/sky-scene-infra`

- [ ] **Step 1: 跑 typecheck + build**

```bash
cd /d/ZYY\ Project/app
npm run typecheck
npm run build
# 期望: 全部通过
```

- [ ] **Step 2: 切回 base 合并（按 §9.1 合并顺序：product-logic → sky-scene-infra）**

```bash
cd /d/ZYY\ Project
git checkout feature/scroll-optimization
# 同步 product-logic 已合入状态
git merge --no-ff round6/product-logic --ff-only 2>/dev/null || echo "product-logic already merged, skip"
git merge --no-ff round6/sky-scene-infra -m "Merge round6/sky-scene-infra: SkyScene + reduced-motion"
# 期望: 无冲突（独占新增文件 + index.css 顶部插入）
```

- [ ] **Step 3: 跑全量测试**

```bash
cd /d/ZYY\ Project/app
npm test
npm run typecheck
npm run build
# 期望: PASS
```

---

## Task 11：today-integration — toCloudGardenMood 工具

**Branch:** `round6/today-integration`
**Files:**
- Create: `app/src/utils/cloudGardenMood.ts`

- [ ] **Step 1: 实现 toCloudGardenMood**

`app/src/utils/cloudGardenMood.ts`：

```ts
import type { SkyMood } from '../components/sky/SkyScene';
import type { CloudMood } from '../types';

export type GardenMood = 'calm' | 'happy' | 'celebrate';

/**
 * M3: 将 SkyMood + 完成态 映射到 CloudGarden.mood
 * 禁止在调用方散写三元（spec §3.6 强制）
 */
export function toCloudGardenMood(skyMood: SkyMood, isCompleted: boolean): GardenMood {
  if (isCompleted) return 'celebrate';
  if (skyMood === 'golden') return 'happy';
  return 'calm';
}
```

> 注意：CloudGarden.mood 类型在 spec §3.6 是 `'calm' | 'happy' | 'celebrate'`。原 `types.ts:61` 有 `CloudMood = 'calm' | 'happy' | 'celebrate'`，可复用。

- [ ] **Step 2: typecheck 验证**

```bash
cd /d/ZYY\ Project/app
npm run typecheck
# 期望: PASS
```

- [ ] **Step 3: Commit**

```bash
cd /d/ZYY\ Project
git add app/src/utils/cloudGardenMood.ts
git commit -m "feat(cloud): add toCloudGardenMood mapper (M3 强制换算)"
```

---

## Task 12：today-integration — CloudGarden mode='today' 重构

**Branch:** `round6/today-integration`
**Files:**
- Modify: `app/src/components/today/CloudGarden.tsx`（§3.6 重构）

> 移除内嵌背景渐变 / 远山 / 草地 / 硬编码 height: 260；加 mode prop；记忆云 5→7；空状态文案改为 M1 版本。

- [ ] **Step 1: 备份原 CloudGarden.tsx 用于对照**

```bash
cd /d/ZYY\ Project
cp app/src/components/today/CloudGarden.tsx /tmp/CloudGarden.tsx.bak
```

- [ ] **Step 2: 替换整个 CloudGarden.tsx**

`app/src/components/today/CloudGarden.tsx` 替换为：

```tsx
import { useState, useEffect } from 'react';
import type { Task } from '../../types';
import RealCloud from './RealCloud';
import CloudSparkles from './CloudSparkles';
import { toCloudGardenMood } from '../../utils/cloudGardenMood';

export type CloudGardenMode = 'today' | 'garden';
export type { GardenMood } from '../../utils/cloudGardenMood';

const CLOUD_GARDEN_CONFIG = {
  today: {
    maxClouds: 7,
    draggable: false,
    showTodayMainCloud: true,
    showEmptyState: true,
    showCompleteButton: true,
    minOpacity: 0.18,
  },
  garden: {
    maxClouds: 30,
    draggable: true,
    showTodayMainCloud: false,
    showEmptyState: false,
    showCompleteButton: false,
    minOpacity: 0.32,
  },
} as const;

export interface CloudGardenProps {
  mode: CloudGardenMode;
  today?: Task | null;
  last7?: Array<{ date: string; tasks: Task[] }>;
  history?: Record<string, Task[]>;
  onTodayComplete?: () => void;
  onOpenCloud?: (date: string) => void;
  mood: 'calm' | 'happy' | 'celebrate';
}

function getCloudColor(type: Task['type']) {
  switch (type) {
    case 'reading': return 'mint';
    case 'exercise': return 'coral';
    case 'coding': return 'lavender';
    default: return 'warm';
  }
}

export default function CloudGarden({
  mode,
  today,
  last7,
  history: _history,
  onTodayComplete,
  onOpenCloud: _onOpenCloud,
  mood,
}: CloudGardenProps) {
  // 本轮只实现 mode='today'
  if (mode === 'garden') {
    // TODO: implement in Round 7 SkyPage rich garden
    return null;
  }

  const config = CLOUD_GARDEN_CONFIG.today;
  const [sparkles, setSparkles] = useState(false);

  useEffect(() => {
    if (today?.completedAt) {
      setSparkles(true);
      const timer = setTimeout(() => setSparkles(false), 1500);
      return () => clearTimeout(timer);
    }
  }, [today?.completedAt]);

  return (
    <div
      className="clay-cloud-garden"
      style={{
        position: 'relative',
        flex: 1,
        minHeight: 0,
        width: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        overflow: 'hidden',
      }}
    >
      {/* 记忆云 */}
      {last7?.slice(0, config.maxClouds).map((day, i) => (
        <div
          key={day.date}
          className="clay-cloud-drift sky-cloud-drift"
          style={{
            position: 'absolute',
            left: `${10 + (i % 5) * 15}%`,
            bottom: `${15 + Math.floor(i / 5) * 12}%`,
            opacity: Math.max(config.minOpacity, 0.6 - i * 0.06),
            transform: `scale(${0.5 + (i % 4) * 0.1})`,
            zIndex: i,
            animationDelay: `${i * 2}s`,
            animationDuration: `${10 + i * 5}s`,
          }}
        >
          <RealCloud
            size="sm"
            color={getCloudColor(day.tasks[0]?.type || 'other')}
            type={day.tasks[0]?.type || 'other'}
            expression={['happy', 'calm', 'sleep', 'wink', 'neutral', 'tiny-smile', 'peeking'][i % 7] as any}
            state="default"
          />
        </div>
      ))}

      {/* 今日主云 */}
      {config.showTodayMainCloud && (
        <div
          style={{
            position: 'relative',
            zIndex: 10,
            transform: today?.completedAt ? 'scale(1.05)' : 'scale(1)',
            transition: 'transform 0.6s cubic-bezier(0.34, 1.56, 0.64, 1)',
          }}
        >
          <RealCloud
            size="lg"
            color={today ? getCloudColor(today.type) : 'warm'}
            state={today?.completedAt ? 'completed' : 'default'}
            mood={mood}
          />
          <CloudSparkles active={sparkles} />

          {/* 空状态：M1 修订（避免与 §4.2 禁止词重叠） */}
          {!today && config.showEmptyState && (
            <div
              style={{
                position: 'absolute',
                top: '50%',
                left: '50%',
                transform: 'translate(-50%, -50%)',
                textAlign: 'center',
                pointerEvents: 'none',
              }}
            >
              <div style={{ fontSize: 32, marginBottom: 8 }}>☁️</div>
              <p
                style={{
                  fontSize: 14,
                  fontWeight: 600,
                  color: 'var(--ink)',
                  textShadow: '0 2px 4px rgba(255,255,255,0.8)',
                }}
              >
                云朵还没发芽
              </p>
              <p style={{ fontSize: 12, color: 'var(--ink-light)', margin: '4px 0 0' }}>
                选一个轻的开始，今天就够了。
              </p>
            </div>
          )}

          {/* 完成按钮 */}
          {today && !today.completedAt && config.showCompleteButton && onTodayComplete && (
            <button
              onClick={onTodayComplete}
              style={{
                position: 'absolute',
                bottom: '-10%',
                left: '50%',
                transform: 'translateX(-50%)',
                padding: '8px 20px',
                background: 'var(--mint-cloud-cta, #4AB574)',
                color: 'white',
                borderRadius: 20,
                border: 'none',
                fontWeight: 600,
                fontSize: 14,
                boxShadow: '0 4px 12px rgba(74, 181, 116, 0.4)',
                cursor: 'pointer',
                zIndex: 100,
                whiteSpace: 'nowrap',
              }}
            >
              让今天养一朵云
            </button>
          )}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 跑全量测试确认无回归**

```bash
cd /d/ZYY\ Project/app
npm test
# 期望: PASS（既有 RealCloud / CloudSparkles 测试不受影响）
```

- [ ] **Step 4: typecheck**

```bash
cd /d/ZYY\ Project/app
npm run typecheck
# 期望: PASS（这里会发现 TodayPage 调用点缺少 mode='today' prop 的 TS 错误，Task 13 修复）
```

> 如果 typecheck 失败（TodayPage 调用点缺 mode），先在 TodayPage 调用处加 `mode="today"` 临时修复（不 commit），继续 Task 13。

- [ ] **Step 5: Commit**

```bash
cd /d/ZYY\ Project
git add app/src/components/today/CloudGarden.tsx
git commit -m "refactor(cloud): add CloudGarden mode='today' contract + 移除内嵌背景/远山/草地"
```

---

## Task 13：today-integration — TodayPage 接入 SkyScene

**Branch:** `round6/today-integration`
**Files:**
- Modify: `app/src/pages/TodayPage.tsx`（H5 精确文案替换 + §4.1 SkyScene 接入）

- [ ] **Step 1: 备份原 TodayPage.tsx**

```bash
cd /d/ZYY\ Project
cp app/src/pages/TodayPage.tsx /tmp/TodayPage.tsx.bak
```

- [ ] **Step 2: 全文重写 TodayPage.tsx**

`app/src/pages/TodayPage.tsx`：

```tsx
import type { AppState, Task } from '../types';
import type { Mood } from '../components/shared/MoodWidget';
import { copy } from '../utils/copy';
import DailyQuote from '../components/shared/DailyQuote';
import Pomodoro from '../components/shared/Pomodoro';
import TodayDecisionCard from '../components/today/TodayDecisionCard';
import TodayFeedbackStrip from '../components/today/TodayFeedbackStrip';
import { SoftButton } from '../components/ui';
import { type Dispatch, type SetStateAction } from 'react';
import CloudGarden from '../components/today/CloudGarden';
import { SkyScene, SkyHeaderContent, SkyProgressMini } from '../components/sky/SkyScene';
import { toCloudGardenMood } from '../utils/cloudGardenMood';

interface TodayPageProps {
  state: AppState;
  today: string;
  todaysTasks: Task[];
  incompleteTasks: Task[];
  completedTasks: Task[];
  allTodaysTasksDone: boolean;
  atMaxTasks: boolean;
  input: string;
  setInput: Dispatch<SetStateAction<string>>;
  handleAddTask: () => void;
  addWithValue: (value: string) => void;
  handleCompleteTask: (id: string) => void;
  handleDeleteTask: (id: string) => void;
  handleMoodSelect: (mood: Mood) => void;
  handlePomodoroComplete: (sessionType: 'focus' | 'shortBreak' | 'longBreak') => void;
  handleReset: () => void;
  handleEasier: () => void;
  onNavigateToSky: () => void;
  pomodoroExpanded: boolean;
  setPomodoroExpanded: Dispatch<SetStateAction<boolean>>;
  skyMood: import('../utils/skyMood').SkyMood;
}

export default function TodayPage({
  state,
  today,
  todaysTasks,
  incompleteTasks,
  completedTasks,
  allTodaysTasksDone,
  atMaxTasks: _atMaxTasks,
  input: _input,
  setInput: _setInput,
  handleAddTask: _handleAddTask,
  addWithValue,
  handleCompleteTask,
  handleDeleteTask: _handleDeleteTask,
  handleMoodSelect,
  handlePomodoroComplete,
  handleReset: _handleReset,
  handleEasier,
  onNavigateToSky,
  pomodoroExpanded,
  setPomodoroExpanded,
  skyMood,
}: TodayPageProps) {
  void _input; void _setInput; void _handleAddTask; void _handleDeleteTask; void _atMaxTasks; void _handleReset;
  const currentTask = incompleteTasks[0] ?? completedTasks[0] ?? null;

  // 转换 last7 给 CloudGarden
  const last7 = Object.entries(state.history)
    .sort(([a], [b]) => b.localeCompare(a))
    .slice(0, 7)
    .map(([date, tasks]) => ({ date, tasks }));

  return (
    <div
      className="clay-content clay-page"
      style={{ flex: 1, display: 'flex', flexDirection: 'column', minHeight: 0 }}
    >
      {/* === H5 修复后：移除 h1/p 顶部硬编码，由 SkyScene + SkyHeaderContent 接管 === */}
      <div className="w-full max-w-md mx-auto" style={{ flexShrink: 0 }}>
        <SkyScene mood={skyMood} density="comfortable" variant="today">
          <SkyHeaderContent
            title={currentTask ? '今天只做这一小步' : '每天不知道从哪开始？'}
            subtitle={
              currentTask
                ? '完成后，天空会多一朵云'
                : '我帮你把想坚持的事，变成今天能完成的一小步。'
            }
          >
            <SkyProgressMini
              streak={state.streak.current}
              totalClouds={state.log.length}
              mood={skyMood}
              hasTodayCloud={allTodaysTasksDone}
            />
          </SkyHeaderContent>

          <CloudGarden
            mode="today"
            today={currentTask}
            last7={last7}
            onTodayComplete={() => currentTask && handleCompleteTask(currentTask.id)}
            mood={toCloudGardenMood(skyMood, !!currentTask?.completedAt)}
          />
        </SkyScene>
      </div>

      {/* === 今日主行动卡（H5 保留完成态文案 "今天的云已经养好"） === */}
      {currentTask ? (
        allTodaysTasksDone ? (
          <div className="animate-fade-up" style={{ margin: '16px', textAlign: 'center' }}>
            <h2 style={{ fontFamily: 'var(--font-display)', fontSize: 22, fontWeight: 700, color: 'var(--ink)', margin: '0 0 8px' }}>
              今天的云已经养好
            </h2>
            <p style={{ fontSize: 13, color: 'var(--ink-light)', margin: '0 0 16px' }}>
              你只做了一小步，但它已经留下来了。
            </p>
            <SoftButton variant="ghost" size="md" onClick={onNavigateToSky}>
              去看看我的天空
            </SoftButton>
          </div>
        ) : (
          <div className="clay-fade-up">
            <TodayDecisionCard
              task={currentTask}
              onComplete={() => handleCompleteTask(currentTask.id)}
              onEasier={handleEasier}
              onStartPomodoro={() => setPomodoroExpanded((p) => !p)}
            />
          </div>
        )
      ) : null}

      {/* 心情小记 */}
      <div style={{ marginTop: 12, display: 'flex', gap: 8, flexWrap: 'wrap', justifyContent: 'center' }}>
        {['平静', '低落', '一般', '期待', '高兴'].map((m, i) => (
          <button
            key={m}
            className="clay-chip"
            onClick={() => handleMoodSelect(['calm', 'low', 'okay', 'hopeful', 'happy'][i] as Mood)}
            style={{ fontSize: 12 }}
          >
            {['☁️', '🌧', '🌤', '🌈', '☀️'][i]} {m}
          </button>
        ))}
      </div>

      {/* 今日反馈 */}
      {allTodaysTasksDone && (
        <TodayFeedbackStrip completed streak={state.streak.current} total={state.log.length} />
      )}

      {/* 弱化区 */}
      <div className="clay-scroll-area" style={{ flex: 1, overflowY: 'auto', minHeight: 0, paddingBottom: '100px' }}>
        <div className="w-full max-w-md mx-auto" style={{ padding: '8px 16px' }}>
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: 8, marginBottom: 20 }}>
            {[
              { label: '读一点', hint: '读 2 页书', emoji: '📖' },
              { label: '走一走', hint: '出门走走 5 分钟', emoji: '🏃' },
              { label: '写一句', hint: '写一行日记', emoji: '📝' },
              { label: '随便养一朵', hint: '深呼吸三次', emoji: '✨' },
            ].map((s) => (
              <button
                key={s.label}
                className="clay-quick-suggest"
                style={{
                  display: 'flex', alignItems: 'center', gap: 8,
                  padding: '12px 16px', borderRadius: 16,
                  background: 'var(--surface-1)', border: '1px solid var(--hairline)',
                  cursor: 'pointer', transition: 'all 0.2s ease-out',
                }}
                onClick={() => addWithValue(s.hint)}
              >
                <span style={{ fontSize: 20 }}>{s.emoji}</span>
                <span style={{ fontSize: 14, fontWeight: 600, color: 'var(--ink)' }}>{s.label}</span>
              </button>
            ))}
          </div>

          <button onClick={() => setPomodoroExpanded((p) => !p)} className="clay-collapse">
            <span>⏱️ 番茄钟 · {state.pomodoroSessions || 0} 次专注</span>
            <span style={{ fontSize: 11, color: 'var(--ink-faint)' }}>
              {pomodoroExpanded ? '收起 ▲' : '展开 ▼'}
            </span>
          </button>
          {pomodoroExpanded && (
            <div style={{ marginTop: 8, display: 'flex', justifyContent: 'center' }}>
              <Pomodoro onComplete={handlePomodoroComplete} />
            </div>
          )}

          <div style={{ opacity: 0.7, marginTop: 16 }}>
            <DailyQuote />
          </div>

          <div style={{ marginTop: 24, textAlign: 'center', opacity: 0.5 }}>
            <p style={{ color: 'var(--ink-muted)', fontSize: 12, fontFamily: 'var(--font-body)', margin: 0 }}>
              {copy.footer()}
            </p>
          </div>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: typecheck**

```bash
cd /d/ZYY\ Project/app
npm run typecheck
# 期望: PASS
```

- [ ] **Step 4: 跑测试**

```bash
cd /d/ZYY\ Project/app
npm test
# 期望: PASS
```

- [ ] **Step 5: build**

```bash
cd /d/ZYY\ Project/app
npm run build
# 期望: PASS
```

- [ ] **Step 6: Commit**

```bash
cd /d/ZYY\ Project
git add app/src/pages/TodayPage.tsx
git commit -m "feat(today): 接 SkyScene 头部, 改文案 '今天只做这一小步', CloudGarden mode='today'"
```

---

## Task 14：today-integration — 合并准备

**Branch:** `round6/today-integration`

- [ ] **Step 1: 切回 base 合并（§9.1 顺序：sky-scene-infra → today-integration）**

```bash
cd /d/ZYY\ Project
git checkout feature/scroll-optimization
git merge --no-ff round6/sky-scene-infra --ff-only 2>/dev/null || echo "sky-scene-infra already merged, skip"
git merge --no-ff round6/today-integration -m "Merge round6/today-integration: TodayPage 接 SkyScene + CloudGarden mode"
# 期望: 无冲突（独占 TodayPage.tsx / CloudGarden.tsx / cloudGardenMood.ts）
```

- [ ] **Step 2: 跑全量验证**

```bash
cd /d/ZYY\ Project/app
npm test
npm run typecheck
npm run build
# 期望: 全部 PASS
```

- [ ] **Step 3: 视觉冒烟（手动）**

```bash
cd /d/ZYY\ Project/app
npm run dev
# 打开 http://localhost:5173
# 验证:
# - 首屏标题是 "今天只做这一小步"（**不**是"今天，从一朵云开始"）
# - 副标题是 "完成后，天空会多一朵云"（**不**带句号）
# - CloudGarden 显示主云 + 记忆云
# - 完成态按钮是 "去看看我的天空" / "明天再来"（**不**是"再养一朵"）
```

---

## Task 15：docs-ci-deploy — CI workflow + engines 字段

**Branch:** `round6/docs-ci-deploy`
**Files:**
- Create: `app/.github/workflows/ci.yml`
- Modify: `app/package.json`（M9 engines + M12 typecheck 脚本）

- [ ] **Step 1: 修改 package.json**

`app/package.json` 加 `engines` 字段和 `typecheck` 脚本：

```json
{
  "name": "daily-zero-decision",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  ...
}
```

- [ ] **Step 2: 创建 CI workflow**

`app/.github/workflows/ci.yml`：

```yaml
name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: app

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: app/package.json
          cache: npm
          cache-dependency-path: app/package-lock.json

      - run: npm ci
      - run: npm test
      - run: npm run typecheck
      - run: npm run build
```

- [ ] **Step 3: 验证**

```bash
cd /d/ZYY\ Project
cat app/package.json | grep -A2 engines
cat app/.github/workflows/ci.yml
# 期望: 都正确
```

- [ ] **Step 4: Commit**

```bash
cd /d/ZYY\ Project
git add app/package.json app/.github/workflows/ci.yml
git commit -m "ci: add GitHub Actions workflow (M9 engines + M12 typecheck)"
```

---

## Task 16：docs-ci-deploy — README + CHANGELOG

**Branch:** `round6/docs-ci-deploy`
**Files:**
- Modify: `app/README.md`（§6.1 新定位）
- Create: `app/CHANGELOG.md`（L1 收口）

- [ ] **Step 1: 重写 README.md**

`app/README.md`：

```markdown
# 每日零决策卡 ☁️

> 每天不知道从哪开始？
> 把想坚持的事，变成今天能完成的一小步。
> 完成这一小步，你的天空就多一朵云。

这不是 Todo，不是番茄钟，也不是习惯统计器。
它每天帮你把想坚持的事，压成一张小到不会抗拒的行动卡。

## 功能

- **今日卡**：把想坚持的事变成最小行动
- **我的天空**：完成后长出一朵云
- **回顾**：用自然语言看见自己慢慢坚持
- **设置**：字体、数据导入导出、重置

## 可选小工具

- **番茄钟**：需要时开始计时，不打扰主流程。

## 快速开始

### 环境要求

- Node.js 20+
- npm

### 安装与开发

```bash
cd app
npm install
npm run dev          # http://localhost:5173
```

### 测试与构建

```bash
npm test             # 运行所有测试
npm run typecheck    # TypeScript 类型检查
npm run build        # → dist/
```

## 技术栈

- React 18 + TypeScript
- Vite 5
- Tailwind CSS
- Vitest

## 设计系统

- **暖奶油画布** — 柔和背景
- **软圆 3D** — claymorphism
- **主题型壳 + 任务型核** — 阅读云 / 散步云 / 编码云 / 日常云

## 许可证

MIT
```

- [ ] **Step 2: 创建 CHANGELOG.md**

`app/CHANGELOG.md`：

```markdown
# Changelog

## v0.1.0 — 2026-06-14 — 每天只做这一小步

### 新增

- 一屏式 Onboarding
- 今日卡重塑为最小行动卡（4 种云主题）
- 完成后天空长出一朵云
- SkyScene 天空场景基础（density/variant/reduced-motion）
- SkyHeaderContent + SkyProgressMini 顶部信息层
- CloudGarden mode='today' 重构（记忆云 5→7）
- toCloudGardenMood 工具
- useReducedMotion hook + 全局 CSS 兜底
- parseTaskFromInput time 推算（6 用例全支持）
- handleEasier 四态 MAX 守卫
- importState / loadState 老用户 backfill + onboarded 三态
- handleOnboardingFinish 显式 saveState
- GitHub Actions CI（typecheck + test + build）
- 仓库根 vercel.json 部署配置
- window.onerror 最小监控

### 修复

- 老用户 type='other' 误归类被 handleEasier 误判
- 完成 1 张卡后再点"换一朵轻一点的"会生成第 2 张
- 完成后出现"再养一朵"违反产品定位

### 文档

- README 重写为"最小行动"定位
- 番茄钟降级为可选小工具

## 上线验收

- 11 条上线验收标准全部通过
- 5 个并发分支无冲突合并
- CI 通过
```

- [ ] **Step 3: Commit**

```bash
cd /d/ZYY\ Project
git add app/README.md app/CHANGELOG.md
git commit -m "docs: align README with minimum action + add CHANGELOG for v0.1.0"
```

---

## Task 17：docs-ci-deploy — vercel.json 部署

**Branch:** `round6/docs-ci-deploy`
**Files:**
- Create: `vercel.json`（仓库根）

- [ ] **Step 1: 创建 vercel.json（仓库根）**

`vercel.json`（路径 = `/d/ZYY Project/vercel.json`，**不带 `app/` 前缀**）：

```json
{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd app && npm run build",
  "outputDirectory": "app/dist",
  "installCommand": "cd app && npm ci",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/app/dist/$1" }
  ]
}
```

- [ ] **Step 2: Commit**

```bash
cd /d/ZYY\ Project
git add vercel.json
git commit -m "ci: add vercel.json for Vercel deployment"
```

---

## Task 18：docs-ci-deploy — SettingsPage v0.1.0 区块 + App.tsx 一次性浮层

**Branch:** `round6/docs-ci-deploy`
**Files:**
- Modify: `app/src/pages/SettingsPage.tsx`（§14.6 第 1 条）
- Modify: `app/src/App.tsx`（§14.6 第 2 条）

- [ ] **Step 1: 读 SettingsPage.tsx 现状**

```bash
cd /d/ZYY\ Project
head -50 app/src/pages/SettingsPage.tsx
```

- [ ] **Step 2: 在 SettingsPage 顶部插入 v0.1.0 更新区块**

在 SettingsPage 函数组件 return 的最顶部添加：

```tsx
<section style={{ padding: '16px', margin: '0 0 16px', background: 'rgba(255, 230, 180, 0.3)', borderRadius: 16, border: '1px solid rgba(255, 200, 100, 0.4)' }}>
  <h2 style={{ fontSize: 16, fontWeight: 700, margin: '0 0 8px', color: 'var(--ink)' }}>
    🎉 v0.1.0 更新
  </h2>
  <ul style={{ fontSize: 13, color: 'var(--ink-light)', margin: 0, paddingLeft: 20, lineHeight: 1.8 }}>
    <li>首屏重塑："今天只做这一小步"</li>
    <li>每日只生成 1 张卡（最多）</li>
    <li>新增 SkyScene 天空场景</li>
    <li>完成后只显示"去看看我的天空 / 明天再来"</li>
    <li>支持 reduced-motion 减少动画</li>
  </ul>
  <p style={{ fontSize: 11, color: 'var(--ink-faint)', margin: '8px 0 0' }}>
    详见 <code>app/CHANGELOG.md</code>
  </p>
</section>
```

- [ ] **Step 3: 修改 App.tsx 加一次性浮层**

`app/src/App.tsx` 找到 Onboarding 后的位置，添加 useEffect：

在 `App` 函数顶部加：

```tsx
const [showChangelogOverlay, setShowChangelogOverlay] = useState(false);

useEffect(() => {
  if (state.onboarded && state.log.length > 0) {
    const shown = localStorage.getItem('daily-zero-decision:lastShownChangelog');
    if (shown !== 'v0.1.0') {
      setShowChangelogOverlay(true);
    }
  }
}, [state.onboarded, state.log.length]);
```

在 `return` 内 `<!-- Completion note modal -->` 之前加：

```tsx
{showChangelogOverlay && (
  <div
    style={{
      position: 'fixed', inset: 0, zIndex: 9999,
      background: 'rgba(0,0,0,0.4)', display: 'flex',
      alignItems: 'center', justifyContent: 'center', padding: 16,
    }}
    onClick={() => {
      localStorage.setItem('daily-zero-decision:lastShownChangelog', 'v0.1.0');
      setShowChangelogOverlay(false);
    }}
  >
    <div
      onClick={(e) => e.stopPropagation()}
      style={{
        background: 'var(--surface-1, white)', borderRadius: 24, padding: 32,
        maxWidth: 360, width: '100%', textAlign: 'center',
      }}
    >
      <h2 style={{ fontSize: 20, fontWeight: 700, margin: '0 0 8px', color: 'var(--ink)' }}>
        🎉 v0.1.0 上线了
      </h2>
      <p style={{ fontSize: 14, color: 'var(--ink-light)', margin: '0 0 16px', lineHeight: 1.6 }}>
        首屏换成"今天只做这一小步"了。
        <br />
        每天只生成一张卡，完成后去看看天空。
      </p>
      <button
        onClick={() => {
          localStorage.setItem('daily-zero-decision:lastShownChangelog', 'v0.1.0');
          setShowChangelogOverlay(false);
        }}
        style={{
          padding: '10px 24px', borderRadius: 16, border: 'none',
          background: 'var(--mint-cloud-cta, #4AB574)', color: 'white',
          fontWeight: 600, cursor: 'pointer',
        }}
      >
        开始
      </button>
    </div>
  </div>
)}
```

确保 import `useEffect` from 'react'。

- [ ] **Step 4: typecheck + build**

```bash
cd /d/ZYY\ Project/app
npm run typecheck
npm run build
# 期望: PASS
```

- [ ] **Step 5: Commit**

```bash
cd /d/ZYY\ Project
git add app/src/pages/SettingsPage.tsx app/src/App.tsx
git commit -m "feat(docs): add v0.1.0 SettingsPage section + App.tsx one-time changelog overlay"
```

---

## Task 19：docs-ci-deploy — main.tsx window.onerror 监控

**Branch:** `round6/docs-ci-deploy`
**Files:**
- Modify: `app/src/main.tsx`（M2 强制唯一挂载点）

- [ ] **Step 1: 读 main.tsx**

```bash
cd /d/ZYY\ Project
cat app/src/main.tsx
```

- [ ] **Step 2: 在 ReactDOM.createRoot 之前添加 window.onerror**

`app/src/main.tsx` 在文件顶部（import 之后、createRoot 之前）插入：

```tsx
// === Round 6 M2: window.onerror 唯一挂载点 ===
// 禁止在 index.html 内联 <script> 重复挂载，禁止在 App.tsx 二次挂载
window.addEventListener('error', (event) => {
  console.error('[window.onerror]', event.error ?? event.message);
});
window.addEventListener('unhandledrejection', (event) => {
  console.error('[unhandledrejection]', event.reason);
});
```

- [ ] **Step 3: typecheck**

```bash
cd /d/ZYY\ Project/app
npm run typecheck
# 期望: PASS
```

- [ ] **Step 4: Commit**

```bash
cd /d/ZYY\ Project
git add app/src/main.tsx
git commit -m "feat(monitoring): window.onerror 唯一挂载点 (M2 强制)"
```

---

## Task 20：docs-ci-deploy — 合并准备

**Branch:** `round6/docs-ci-deploy`

- [ ] **Step 1: 切回 base 合并**

```bash
cd /d/ZYY\ Project
git checkout feature/scroll-optimization
git merge --no-ff round6/today-integration --ff-only 2>/dev/null || echo "today-integration already merged, skip"
git merge --no-ff round6/docs-ci-deploy -m "Merge round6/docs-ci-deploy: CI + README + vercel + changelog + monitoring"
# 期望: 无冲突（独占新增/修改文件）
```

- [ ] **Step 2: 跑全量验证**

```bash
cd /d/ZYY\ Project/app
npm test
npm run typecheck
npm run build
# 期望: 全部 PASS
```

---

## Task 21：qa-polish — 完整 QA 矩阵回归

**Branch:** `round6/qa-polish`
**Files:**
- 不新增任何文件（spec §6.x H1 收口：qa-polish 不写新测试文件，只跑 + 改既有测试）
- 可能修改 `app/vitest.config.ts`（可选 L3）或 `app/src/test/setup.ts`（如果需要）

- [ ] **Step 1: 切回 base 同步**

```bash
cd /d/ZYY\ Project
git checkout feature/scroll-optimization
git merge --no-ff round6/docs-ci-deploy --ff-only 2>/dev/null || echo "docs-ci-deploy already merged, skip"
git checkout -b round6/qa-polish
```

- [ ] **Step 2: 跑全量测试**

```bash
cd /d/ZYY\ Project/app
npm test
# 期望: 全部 PASS（storage.test.ts + useTasks.test.ts + useReducedMotion.test.ts + SkyScene.test.tsx + 原 skyMood.test.ts）
```

- [ ] **Step 3: 跑 typecheck**

```bash
cd /d/ZYY\ Project/app
npm run typecheck
# 期望: PASS
```

- [ ] **Step 4: 跑 build**

```bash
cd /d/ZYY\ Project/app
npm run build
# 期望: PASS
```

- [ ] **Step 5: 视觉冒烟（手动，§7.1-7.7）**

```bash
cd /d/ZYY\ Project/app
npm run dev
# 打开 http://localhost:5173，依次验证:
# - §7.1 新用户：清空 localStorage → 看到 Onboarding → 选"读书" → "读 2 页书" → 完成 → "去看看我的天空" → 看见云
# - §7.2 老用户：保留 localStorage → 不重弹 Onboarding → 历史云仍在
# - §7.3 6 用例：分别测试"读 2 页书 / 出门走走 5 分钟 / 看 5 分钟代码 / 背 5 个新词 / 写一行日记 / 深呼吸三次" → type / title / time 全部正确
# - §7.4 一天一张：生成 1 张 → 再点"换一朵轻一点的" → 标题被改写但**不**生成第 2 张
# - §7.5 reduced-motion：系统开启 → 飞鸟/热气球不动
# - §7.6 移动端：devtools 切 320/375/390/430/768 px → 标题不挤压、SkyScene 自动降 minimal
# - §7.7 数据导入：导出 → 清空 → 导入 → streak/log/history 全部恢复
```

- [ ] **Step 6: 修复发现的 bug（如有）**

如果发现 bug，**直接修复**（不 commit 单独 commit），走 §9.1 共享文件归属表的归属规则：

| bug 文件 | 修复方式 |
|---|---|
| `app/src/hooks/useTasks.ts` | qa-polish 直接改（small fix；product-logic 已合入但 spec 允许 qa-polish 修 small bug） |
| `app/src/utils/storage.ts` | qa-polish **禁止**改（产品逻辑层独占；如需改写一个 hotfix 提 PR） |
| `app/src/components/sky/SkyScene.tsx` | qa-polish **禁止**改 |
| `app/src/components/today/CloudGarden.tsx` | qa-polish **禁止**改 |
| `app/src/pages/TodayPage.tsx` | qa-polish **禁止**改 |
| `app/src/components/sky/SkyHeaderContent.tsx` / `SkyProgressMini.tsx` | qa-polish **禁止**改 |

- [ ] **Step 7: 切回 base 合并 qa-polish**

```bash
cd /d/ZYY\ Project
git checkout feature/scroll-optimization
git merge --no-ff round6/qa-polish -m "Merge round6/qa-polish: QA 矩阵回归 + 修复"
# 期望: 无冲突（qa-polish 没新增文件；如有 small fix 也走 small rebase）
```

- [ ] **Step 8: 跑最终全量验证（§9.2 step 7 强制）**

```bash
cd /d/ZYY\ Project/app
npm ci
npm test
npm run typecheck
npm run build
npm run preview
# 期望: 全部 PASS
```

---

## Task 22：合并到 main + tag v0.1.0 + 部署

- [ ] **Step 1: 切到 main 分支**

```bash
cd /d/ZYY\ Project
git checkout main
```

- [ ] **Step 2: 合并 feature/scroll-optimization**

```bash
cd /d/ZYY\ Project
git merge --no-ff feature/scroll-optimization -m "Release v0.1.0: 每天只做这一小步"
# 期望: 全部 6 任务已合入，无冲突
```

- [ ] **Step 3: 在 main 上跑全量验证（§9.2 step 7 强制）**

```bash
cd /d/ZYY\ Project/app
npm ci
npm test
npm run typecheck
npm run build
# 期望: 全部 PASS
```

- [ ] **Step 4: 打 tag v0.1.0**

```bash
cd /d/ZYY\ Project
git tag -a v0.1.0 -m "v0.1.0 — 每天只做这一小步

- 一屏式 Onboarding
- 今日卡重塑为最小行动卡
- 完成后天空长出一朵云
- 新增 SkyScene 天空场景基础
- 支持 reduced-motion
- CI 构建与测试
- vercel.json 部署配置
"
```

- [ ] **Step 5: 推送到 origin（如果远程存在）**

```bash
cd /d/ZYY\ Project
git push origin main --follow-tags 2>/dev/null || echo "remote not set, tag is local"
```

- [ ] **Step 6: 部署（§6.3 Vercel 触发）**

如果 origin 关联 Vercel，推送 tag 会自动触发 Vercel production deploy。

手动部署（Vercel CLI）：
```bash
cd /d/ZYY\ Project
npx vercel --prod
# 期望: 部署成功，URL 返回
```

部署后验证（§13 上线验收 11 条）：
- 打开 Vercel URL
- 首屏标题是"今天只做这一小步"
- 完成一张卡后看"去看看我的天空"
- 刷新页面不白屏
- devtools 切移动端宽度可用

---

## Self-Review（写作完成后自查）

**1. Spec 覆盖检查**：
- [x] §3.1 组件树 → Task 8（SkyScene）+ Task 9（SkyHeaderContent + SkyProgressMini）+ Task 11（cloudGardenMood）+ Task 12（CloudGarden mode='today'）+ Task 13（TodayPage 接入）
- [x] §3.2 SkyScene 类型/density/variant/H12 → Task 8
- [x] §3.3 useReducedMotion + M1 CSS 位置 → Task 6 + Task 7
- [x] §3.4 SkyHeaderContent + L2 句号 → Task 9
- [x] §3.5 SkyProgressMini 三类文案 + mood→icon → Task 9
- [x] §3.6 CloudGarden mode + 配置表 + 移除项 + 空状态 M1 + Sun/Moon M2 + mood 映射 M3 → Task 11 + Task 12
- [x] §4.1 接入示例 + §4.2 文案硬约束 + H5 替换清单 → Task 13
- [x] §5.1 parseTaskFromInput time 推算 + Partial<Task> 警告 → Task 2
- [x] §5.2 handleEasier makeEasierTitle → Task 12（含在 makeEasierTitle 注释）
- [x] §5.3 H4 四态守卫 → Task 3
- [x] §5.4 importState backfill + H9 三态 + L5 显式 saveState → Task 2 + Task 3 + Task 4
- [x] §6.1 README + 路径命名约定 + CHANGELOG 路径 → Task 16
- [x] §6.2 CI workflow + engines + typecheck → Task 15
- [x] §6.3 vercel.json → Task 17
- [x] §6.4 M2 唯一 window.onerror 挂载点 → Task 19
- [x] §6.x 测试基线 4 文件归属 → Task 2 + Task 3 + Task 6 + Task 8
- [x] §7 QA 矩阵 → Task 21（qa-polish）
- [x] §9.1 共享文件归属表 → 每个 Task 的 "Files" 都已标注
- [x] §9.2 合并顺序 → Task 1 + Task 5 + Task 10 + Task 14 + Task 20 + Task 22
- [x] §10 commit 规范 → 每个 Task 的 commit message 都符合 `feat|fix|refactor|ci|docs(scope):` 格式
- [x] §11 默认决策 → 散落在 Task 6/7/8/12/13
- [x] §13 上线验收 11 条 → Task 21 + Task 22 step 6
- [x] §14.6 一次性浮层 + SettingsPage 区块 → Task 18

**2. Placeholder 扫描**：
- 无 TBD / TODO 占位（"TODO: implement in Round 7" 是 spec 设计意图，不是占位）
- 无"add appropriate error handling"
- 无"similar to Task N"（每个 Task 都有完整代码）
- 无描述无代码的 step

**3. 类型一致性**：
- `SkyMood` 在 SkyScene / SkyHeaderContent / SkyProgressMini / cloudGardenMood / SkyProgressMini.test 全部对齐
- `CloudGardenMode` 在 CloudGarden / today-integration 全部对齐
- `GardenMood = 'calm' | 'happy' | 'celebrate'` 在 cloudGardenMood / CloudGarden / TodayPage 全部对齐
- `MAX_TASKS_PER_DAY` 在 useTasks.ts 仅在 product-logic 改写，跨 Task 引用一致

**4. 文件路径一致性**：
- 仓库根 = `D:/ZYY Project`（git）
- 子项目根 = `app/`
- 所有"项目内"路径带 `app/` 前缀
- `vercel.json` 在仓库根（不带 `app/`）

---

## 执行选项

**Plan complete and saved to `app/docs/superpowers/plans/2026-06-14-round6-ship.md`. 22 tasks, 约 5-6 小时实施量（5 并发分支可压缩到 1.5-2 小时）。**

**Two execution options:**

1. **Subagent-Driven (recommended)** — 派发 5 个 subagent 各跑一个分支，任务间 review
2. **Inline Execution** — 当前 session 内串行执行所有任务，带 checkpoint 让你 review

**Which approach?**

如果你选 1，我会派发 5 个并发 subagent（每个跑自己分支的所有 task）然后 review 合并顺序。**风险**：subagent 不一定严格遵守 §9.1 共享文件归属表，可能出现并发冲突。

如果你选 2，我会在关键节点（Task 5/10/14/20/22）停下来让你 review 视觉冒烟和合并结果。**更稳**但慢。