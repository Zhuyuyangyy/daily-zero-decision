<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.secondhand.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.campus.secondhand.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="school" property="school"/>
        <result column="student_id" property="studentId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 按用户名查询 -->
    <select id="getByUsername" parameterType="string" resultMap="UserResultMap">
        select * from user where username = #{username}
    </select>

    <!-- 按手机号查询 -->
    <select id="getByPhone" parameterType="string" resultMap="UserResultMap">
        select * from user where phone = #{phone}
    </select>

    <!-- 插入用户 -->
    <insert id="insert" parameterType="com.campus.secondhand.entity.User" useGeneratedKeys="true" keyProperty="id">
        insert into user (username, password, nickname, phone, email, school, student_id, status, create_time, update_time)
        values (#{username}, #{password}, #{nickname}, #{phone}, #{email}, #{school}, #{studentId}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <!-- 按ID查询 -->
    <select id="selectById" parameterType="long" resultMap="UserResultMap">
        select * from user where id = #{id}
    </select>

    <!-- 按ID更新 -->
    <update id="updateById" parameterType="com.campus.secondhand.entity.User">
        update user
        set nickname = #{nickname},
            phone = #{phone},
            email = #{email},
            school = #{school},
            student_id = #{studentId},
            status = #{status},
            update_time = #{updateTime}
        where id = #{id}
    </update>
</mapper>